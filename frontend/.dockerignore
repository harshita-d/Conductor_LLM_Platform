# --- Node / Tooling ---
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json.*        # keep the real lockfile, ignore backups
yarn.lock.*                # keep the real lockfile, ignore backups
pnpm-lock.yaml.*           # keep the real lockfile, ignore backups

# --- Build outputs (never send local builds to the image) ---
build
dist
.storybook-out
storybook-static

# --- Framework caches ---
.cache
.parcel-cache
.vite
.next
out
.turbo

# --- Test artifacts (optional—keep if you run tests in Docker) ---
coverage
.cypress-cache
cypress/screenshots
cypress/videos
playwright-report
test-results

# --- Env & local configs ---
.env
.env.*
!.env.example              # allow a template if you have one
*.local
*.swp
*.swo

# --- OS / VCS / Editor ---
.DS_Store
Thumbs.db
.git
.gitignore
.gitattributes
.github
.vscode
.idea

# --- Docker / Repo meta (not needed inside image) ---
Dockerfile*
docker-compose*.yml
README.md
LICENSE
